<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Titre</title>
  </head>

  <body>
    {% if added %} House added!{% endif %}
    
    <form action="." method="post">
      {% csrf_token %}
      {{ form.as_p }}
      {{ contributor_form.as_p }}
    <input type="submit" value="Submit" />
    </form>
  </body>
</html>
